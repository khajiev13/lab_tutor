/**
 * DatasetSwitcher — retired.
 *
 * The ARCD Agent now operates in API-only mode, fetching student portfolios
 * directly from the Lab Tutor Knowledge Graph backend.  Multi-dataset
 * switching (XES3G5M / Junyi / EdNet training datasets) is no longer exposed
 * in the UI.  This file is kept as a safe empty export so that any orphaned
 * import does not break the build.
 */
export default function DatasetSwitcher() {
  return null;
}
