"""Document extraction + Neo4j insertion feature module.

This module owns the runtime extraction pipeline (teacher-uploaded documents -> structured
DTOs) and insertion into Neo4j using the official neo4j driver.
"""
